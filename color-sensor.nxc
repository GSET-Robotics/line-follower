  task main() {
    unsigned int c;

    SetSensorType(S1, SENSOR_TYPE_LOWSPEED);

    while (true) {

      c = SensorHTColorNum(S1);

      ClearLine(LCD_LINE3);
      NumOut(10,LCD_LINE3,c);

    }

  }
